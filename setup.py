from setuptools import setup

setup(
    name = 'internal-utils',
    version = '20161211',
    description = 'internal utility methods',
    packages = [
        'internal',
    ],
    install_requires = [
        'flask'
    ]
)
